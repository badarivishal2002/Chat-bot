module.exports = {
  plugins: {
    tailwindcss: {},
  },
}

// Note: autoprefixer removed temporarily due to npm installation issues
// Tailwind CSS v3.4 handles most vendor prefix requirements automatically
